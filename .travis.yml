language: python
python:
  - "2.7"
env:
    - TOX_ENV=docs
    - TOX_ENV=flake8
    - TOX_ENV=py27
install:
    - pip install tox
    # - pip install coveralls
script:
    - tox -e $TOX_ENV
# after_success:
#     coveralls