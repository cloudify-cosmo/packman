variables:
    x: y

packages:
    mock_package:
        name: test_package
        version: 3.1
        sources_path: sources
        # prereqs:
        #     - curl
        # requires:
        #     - make
        # source_ppas:
        #     - ppa:chris-lea/node.js
        # source_urls:
        #     - https://github.com/jaraco/path.py/archive/master.zip
        # source_repos:
        #     - deb http://nginx.org/packages/mainline/ubuntu/ precise nginx
        #     - deb-src http://nginx.org/packages/mainline/ubuntu/ precise nginx
        # source_keys:
        #     - http://nginx.org/keys/nginx_signing.key
        # modules:
        #     - pyyaml
        package_path: tests/
        source_package_type: dir
        destination_package_type: tar.gz
        keep_sources: true
        test_template_parameter: test_template_output
        # config_templates:
        #     template_file:
        #         path: tests/templates/mock_template.template
        #         output: mock_template
        #         config_directory: